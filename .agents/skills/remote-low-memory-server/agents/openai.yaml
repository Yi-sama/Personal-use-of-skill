interface:
  display_name: "Chengdu and US Servers"
  short_description: "Inspect or operate Chengdu, US, or both servers safely"
  default_prompt: "Use $remote-low-memory-server to inspect the server(s) I specify: Chengdu, US, or both; ask which if unspecified."

policy:
  allow_implicit_invocation: false
